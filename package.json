{
  "name": "typing-svg-api",
  "version": "1.0.0",
  "description": "Typing-style SVG generation API for GitHub README",
  "main": "api/typing.ts",
  "scripts": {
    "dev": "vercel dev",
    "prebuild": "npx ts-node scripts/generateFontData.ts",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "github",
    "readme",
    "svg",
    "typing",
    "animation",
    "vercel"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.27",
    "@vercel/node": "^3.0.0",
    "prettier": "^3.7.4",
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  },
  "dependencies": {
    "@fontsource/press-start-2p": "^5.2.7"
  }
}
